//- Kyan M, Syed Hassnat A
//- Lab 4
extends ../layouts/main

block content
  h1 Welcome to the Animal Preservation System
  h2 The Most Respected Animal Matchmaker for International Zoos
  p The Animal Preservation System is committed to matching possible mates held in captivity for the preservation of their species. We keep a database of the animals available so that you can pair up possible mates for at-risk animals in your home zoo.
  hr
  h2 Add an Animal to the Database
  //- Check if success message exists
  if session.successMessage
    .alert.alert-success= session.successMessage
    //- Clear the success message from session
    session.successMessage=undefined
  form(method='POST', action='/add-animal')
    .mb-3
      label(for='homeZoo') Home Zoo
      input#homeZoo.form-control(type='text', name='homeZoo', required)
    .mb-3
      label(for='scientificName') Scientific Name
      input#scientificName.form-control(type='text', name='scientificName', required)
    .mb-3
      label(for='commonName') Common Name
      input#commonName.form-control(type='text', name='commonName', required)
    .mb-3
      label(for='givenName') Given Name
      input#givenName.form-control(type='text', name='givenName', required)
    .mb-3
      label(for='gender') Gender
      select#gender.form-select(name='gender')
        option(value='Male') Male
        option(value='Female') Female
    .mb-3
      label(for='dateOfBirth') Date of Birth
      input#dateOfBirth.form-control(type='date', name='dateOfBirth', required)
    .mb-3
      label(for='age') Age
      input#age.form-control(type='number', name='age', required, min='0')
    .mb-3
      label(for='isTransportable') Is Transportable
      select#isTransportable.form-select(name='isTransportable')
        option(value='Available') Available
        option(value='Unavailable') Unavailable
    .mb-3
      button.btn.btn-primary(type='submit') Add Animal to Database
      br
      button.btn.btn-secondary(type='reset') Reset Form